services:
  postgres:
    image: postgres:16-alpine
    container_name: arctrade-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: arctrade
      POSTGRES_PASSWORD: arctrade
      POSTGRES_DB: arctrade
    ports:
      - "5432:5432"
    volumes:
      - arctrade_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arctrade -d arctrade"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: arctrade-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - arctrade_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  arctrade_pgdata:
  arctrade_redis:
